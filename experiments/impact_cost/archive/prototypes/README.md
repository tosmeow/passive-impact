# Prototypes

This folder holds standalone exploratory scripts that are not imported by the
active impact-cost package.

- `metaorder_detection.py`: prototype metaorder assignment helper; no active
  code path or test imports it.
