# Diagnostics Archive

This directory keeps exploratory and diagnostic impact-cost scripts that are no
longer part of the canonical lifecycle experiment surface.

They are retained for development history and targeted validation only. New
library-ready lifecycle work should go through `../../load_experiments/`.

Archived diagnostic outputs default to `data/`, which is intentionally ignored
except for its README placeholder.
